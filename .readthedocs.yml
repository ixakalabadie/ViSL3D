version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: "3.10"

sphinx:
  configuration: docs/source/conf.py

python:
  install:
  - method: pip
    path: .
  - requirements: requirements.txt

