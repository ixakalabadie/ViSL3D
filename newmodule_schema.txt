

USER INPUTS: *given or default ** given, default or none

data (array+header data or fits), isolevels*, units*, limits*, galaxies**, image2d**


Class main()
	__init__(self, data, isolevels, units, limits, galaxies, image2d)
	
	CALCULATE COORDINATES and PARAMETERS needed later
	(No write_x3d or write_html classes)

	self.file_x3d = open(name_x3d, 'w')
	self-file_html = open(name_html, 'w')
	
	
	make_layers():
		self.file_x3d.write(Transform and other nodes for the layers)
	
	... other functions for x3d objects
	
	make_html_buttons(**args=bool)
		make or not the buttons in html.
		if all are False make html with just the x3d
		otherwise take user input or make default
		e.g. hide/show are default, colormaps can be input and default		
	
